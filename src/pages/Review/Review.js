import React from 'react';
import useTitle from '../../hooks/useTitle';

const Review = () => {
    useTitle('Review')
    return (
        <div>
            <h2>Reviews Here</h2>
        </div>
    );
};

export default Review;